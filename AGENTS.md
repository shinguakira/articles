# 文章スタイルガイド

## 文のつなぎ方

短文を。で連射しない。「〜で、」「〜するが、」「〜なので、」などで自然につなぐ。

悪い例:
> 原因はブラウザの翻訳機能です。ブラウザ翻訳を使うのはUIと母語が違うユーザーです。再現しにくいバグになります。

良い例:
> 原因はブラウザの翻訳機能で、ブラウザ翻訳を使うのはUIと母語が違うユーザーなので、特定のユーザーだけで起き、再現しにくいバグになる。

## AI臭い言い回しを避ける

以下のような表現は使わない。

- 「〜することを推奨します」→ 「〜する」「まずここを読む」
- 「〜の位置づけです」→ 具体的に何なのかを書く
- 「〜を紹介しています」→ 「〜をまとめている」「〜について書いている」
- 「〜網羅的に解説している」→ 使ってもいいが多用しない
- 「一次情報として」→ 不要、削る

## 英語由来の言い回しを避ける

日本語として定着していないカタカナ語や、英語を直訳したような表現を避ける。

- 「補助的な位置づけ」→ 「完全には防げない、あくまで補助」

## 参考リンクの扱い

参考を平等に並べない。記事間の関係（どれが一次情報か、どれがどれを参照しているか）を明記する。

- 最重要の記事は最初に置き、なぜ重要かを一言添える
- 他の記事がそれを参照している場合はその旨を書く
- 本文中で詳しく説明しているものはリンクを参考に入れない
- 確認していない関係性を「以下はすべて〜」のようにまとめない

---

## 記事構造のパターン

### 技術バグ・仕様調査系

現象（エラーメッセージや再現条件を先に見せる）→ 原因 → 正しい使い方/対策 → まとめ → 参考

参考は最後に置くのが基本だが、特定の記事が前提知識になる場合は早めに出してもよい（Reactブラウザ翻訳バグ記事のように）。

### 設計・意見系

問題提示 → 観点別考察（開発視点・採用視点・費用視点など） → 結論

### まとめセクション

箇条書きで要点を3〜5個に絞り、一言で言い切る。冗長な補足は入れない。

---

## コードブロックの書き方

- ❌/✅ で悪い例・良い例を対比する
- コード内コメントは日本語で書く（`// 現在時刻でOK`、`// ここを見れば〜`）
- コードの直前か直後に1文説明を置く。コードだけ貼らない

---

## 文体・語調

### 基本

- です/ます ベースが多いが、技術的な事実の説明では体言止めや終止形も使う（記事によってどちらかに統一する）
- 意見は「〜かと思います」「〜と思っています」で率直に言う。断定を避けたいときに使う
- 事実の断定は「〜です」「〜になる」で言い切る。濁さない

### よく使う接続パターン

- 「〜なので、」「〜なため、」で因果をつなぐ
- 「〜ですが、」「〜するが、」で逆接
- 「〜だけでなく、〜も」で並列

### 強調

- 重要な概念は **bold** または `コード` スタイル
- 重要な一文は blockquote（`>`）で抜き出す
- セクション冒頭に要点を一言置いてから詳細に入る

### 避けるべきパターン

- 「〜することができます」→ 「〜できます」に短縮
- 「〜という形になります」→ 「〜になる」
- 同じ語尾（「〜です。〜です。〜です。」）が3回以上続く
